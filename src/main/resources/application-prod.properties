spring.config.activate.on-profile=prod

# PostgreSQL datasource configuration. Replace env vars or set via deployment config.
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:crafting}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:password}
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=validate

# Enable Flyway migrations in prod; baseline on migrate is useful for existing databases
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true

management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized

# Owner Discord snowflake ID — MUST be set via OWNER_DISCORD_ID environment variable
owner.discord-id=${OWNER_DISCORD_ID}

# CORS — comma-separated frontend origins
cors.allowed-origins=${CORS_ORIGINS:http://localhost:5173}
